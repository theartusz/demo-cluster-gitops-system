apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: angular
  namespace: angular
spec:
  imageRepositoryRef:
    name: angular
    namespace: angular
  filterTags:
    pattern: '^develop-[a-zA-Z0-9]+-(?P<gh_run>.*)'
    extract: '$gh_run'
  policy:
    numerical:
      order: asc